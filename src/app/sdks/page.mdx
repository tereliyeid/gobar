import { Libraries } from '@/components/Libraries'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'Gotopup SDKs',
  description:
    'Kami menyediakan Library PHP yang telah disesuaikan untuk memudahkan Anda dan memberikan pengalaman terbaik saat menggunakan API.',
}

export const sections = [
  { title: 'Official libraries', id: 'official-libraries' },
]

<HeroPattern />

# Gotopup SDKs

Kami direkomendasikan untuk terhubung dengan API menggunakan salah satu SDK resmi kami. Saat ini, Kami hanya menyediakan Library PHP yang telah disesuaikan untuk memudahkan Anda dan memberikan pengalaman terbaik saat menggunakan API. {{ className: 'lead' }}

<Libraries />
